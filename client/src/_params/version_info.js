const setupVersionInfo = (state) => {

  state.version = {}
  state.version.number = "v1.0.4"
  state.version.date = "29th Nov 2017"
  console.log("Retrieved version info")

}

export default setupVersionInfo
