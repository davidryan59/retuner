const playPrevNote = (state, key) => {
  // Need to implement something to
  // play the previous note in the history,
  // and rewind back 1 place.

  // If there is no previous note, play same note, do not change the history.
  console.log("Play previous note: NOT IMPLEMENTED YET");

}

export default playPrevNote
