const playNextNote = (state, key) => {
  // Need to implement something to
  // play the next note in the history,
  // and go forward 1 place.

  // If these is no next note, repeat current note, but add nothing to the history.
  console.log("Play next note: NOT IMPLEMENTED YET");

}

export default playNextNote
