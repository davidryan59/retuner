// WORK IN PROGRESS
const updateAllIntervals = state => {
  console.log('WORK IN PROGRESS - NEED TO UPDATE ALL KEY NOTATIONS')
  for (const key of state.key.array) {
    if (key.transposes) {
      // recalcKeyNotations(state, key)      
    }
  }
}

export default updateAllIntervals
